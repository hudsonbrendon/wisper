# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
/src-tauri/target/

# OpenWispr runtime data — model files are downloaded at runtime, never commit them
*.bin

# test coverage
coverage

# Local environment variables (never commit real credentials)
.env

# Stray root-level Deno lockfile (the real one lives at supabase/functions/deno.lock)
/deno.lock

.superpowers/

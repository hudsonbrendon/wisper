# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
/src-tauri/target/

# OpenWispr runtime data — model files are downloaded at runtime, never commit them
*.bin

# test coverage
coverage

# Local environment variables (never commit real credentials)
.env

# Leftover Deno lockfile from the removed Supabase edge functions. Nothing in
# the repo uses Deno any more; this just keeps the stray file out of git status.
/deno.lock

.superpowers/
